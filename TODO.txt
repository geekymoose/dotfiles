(B) Open "find references" (`gr` shortcut) in Telescope (instead of the default ugly UI) @telescope @telescope @search @plugin @dev @ui +Neovim
(A) Add a shortcut to start `grep_string` in Telescope (find word under selector) @telescope @plugin @shortcuts @search +Neovim
(D) Show file line number in the Telescope preview @telescope @plugin @dev +Neovim
(B) In the search, display only the filename (not the matched line) @telescope @plugin @search +Neovim
(D) In the search, change `<C-d>` and `<C-u>` to move the search (not the preview) @telescope @plugin @search @shortcuts +Neovim
(B) Add a setting to specify, for a project, what to include / exclude @telescope @plugin @search +Neovim
(B) Add harpoon in vscode (See https://github.com/ThePrimeagen/harpoon) @navigation @plugin +Neovim
(C) Add a way to close buffers from telescope @shortcuts @telescope @navigation @plugin +Neovim
(D) Unset the nvim-tree keymap `<C-e>` @shortcuts @plugin +Neovim
(B) Add a plugin to help nvim development @plugin @dev +Neovim
(D) Improve the `lua` configuration so that `plugins` files can be imported several times @plugin @dev +Neovim