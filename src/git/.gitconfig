[user]
    name                = geekymoose
    email               = constantin.masson@umontreal.ca

[push]
    default             = simple
    recurseSubmodules   = on-demand

[pull]
    recurseSubmodules   = on-demand
    rebase              = false

[credential]
    helper              = cache

[core]
    pager               = delta

[interactive]
    diffFilter          = delta --color-only

[diff]
    colorMoved          = true

[merge]
    tool                = vimdiff
    conflictstyle       = diff3

[mergetool]
    keepBackup          = false

[mergetool "vimdiff"]
    cmd                 = nvim -d "$LOCAL" "$BASE" "$REMOTE" "$MERGED" -c '$windcmd w' -c 'wincmd J'

[delta]
    line-numbers        = true
    side-by-side        = true
    navigate            = true      # use n and N to move between diff sections
    light               = false     # set to true if you're in a terminal w/ a light background color (e.g.

[alias]
    s                   = status
    logone              = log --oneline --decorate --graph --branches
    logones             = log --oneline --decorate --graph --branches --format=\"%h %an %ae\"

