[module/filesystem]
type = internal/fs
interval = 25

mount-0 = /

format-mounted-background = ${colors.label}
format-mounted-foreground = ${colors.white}

format-mounted-prefix = %{T2}%{T-}
format-mounted-prefix-background = ${colors.prefix}
format-mounted-prefix-foreground = ${colors.white}
format-mounted-prefix-padding = 1

label-mounted = %percentage_used%%
label-unmounted = %mountpoint% not mounted
label-unmounted-foreground = ${colors.foreground-alt}
